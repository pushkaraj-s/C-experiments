#include <stdio.h>

    //Theory qustion
 
int main(){
    
    //You have to store the marks of 30 students n class, what will you use?
    // a. Array of floats
    // b. Structures
    
    /*
    Ans: We can solve this question using both mentords a and b.
    Logical choice here will be to use a. Array of floats.

    As we are storing same type of values it is better to use array instad of struct.
    Structures are mainly used to combine multiple types and create our own type.

    Experienced programmer will always select array of floats for same data types.
    
    */
   return 0;
}
 